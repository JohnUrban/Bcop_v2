These scripts were run in the same directory as NCBI dataset processing for EGGS.
- There needed to be some adjustments to processed files set up files for GeneSpace.
- All files called upon will exist in that directory after running the "process" scripts successfully.
